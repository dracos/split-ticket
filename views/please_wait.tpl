% rebase('base.tpl')

<h2>{{ fr_desc }} to {{ to_desc }},
% if via:
via {{ via }},
% end
% if day:
for the day,
% end
leaving around {{ time }}
</h2>

<div class="results">
Just doing the calculating...
</div>
